# S4 – Codes and Scripts

This folder contains the codes and environment specifications used to reproduce results from the manuscript:  
**"Generative AI uncovers novel Chrebp/Txnip axis inhibitors with potential anti-inflammatory activity" (ci-2025-02172q)**

---

## Environment Setup

The required dependencies are listed in `environment.yml`.  
To create and activate the environment:

```bash
conda env create -f environment.yml
conda activate env



Notes

Scripts were tested with Python 3.6 and TensorFlow-GPU 2.0.0

A CUDA-enabled GPU is required for model training and generation

For figure reproduction, see scripts in scripts/ with filenames corresponding to manuscript figures


## CODES

